# Architecture (placeholder)

This repository contains the Agent (PowerShell modules) and Server (ASP.NET Core) components. A consolidated architecture overview has been moved to the canonical docs under `docs/shared/`.

If you need a detailed architecture diagram here, tell me what format you prefer (Mermaid, PNG, PlantUML) and I will generate it and place it in `docs/`.
