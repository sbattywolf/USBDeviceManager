# AILocalModelLibrary (workspace seed)

This folder is a local scaffold for the AILocalModelLibrary packaging of the `.continue` tooling.

Purpose
- Provide a minimal, repo-contained layout that can be pushed to https://github.com/sbattywolf/AILocalModelLibrary.
- Contain the scripts, a PowerShell module manifest, and a simple `get-continue.ps1` bootstrap stub.

What is included here
- `AILocalModelLibrary.psd1` — module manifest (placeholder).
- `get-continue.ps1` — bootstrap script to copy/clone the tools into a target repository.
- `README.md` — this file.

Next steps
- Review and adapt the included scripts for distribution.
- Push to the remote `sbattywolf/AILocalModelLibrary` repository and publish instructions in the README.
