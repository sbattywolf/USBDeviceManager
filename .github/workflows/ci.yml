name: CI

on:
  push:
    branches: [ main, master, '**/chore/**', '**/feat/**' ]
    # Avoid running CI for changes that only affect docs, artifacts, or non-code assets
    paths-ignore:
      - 'docs/**'
      - 'design/**'
      - 'artifacts/**'
      - 'publish/**'
      - 'screenshots/**'
      - 'test-results/**'
      - 'logs/**'
      - '*.md'
  pull_request:
    branches: [ main, master ]

env:
  CI_BIND_ADDRESS: 127.0.0.1

jobs:
  gui-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install system GUI dependencies
        run: |
          set -e
          sudo apt-get update
          # Install Mesa/GL packages commonly required by headless PyQt/OpenGL users.
          # Relax package list to use packages available on newer Ubuntu runners,
          # and include `mesa-utils` as a fallback.
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            libgl1-mesa-dri libgbm1 xvfb mesa-utils \
            libx11-6 libxrender1 libxext6 libxcb1 libxkbcommon0 \
            ca-certificates || true
          sudo apt-get clean || true

      - name: Install GUI requirements
        run: |
          python -m pip install --upgrade pip
          pip install -r gui/requirements.txt

      - name: Run GUI tests (headless with Xvfb)
        env:
          PYTHONPATH: ${{ github.workspace }}
          QT_QPA_PLATFORM: offscreen
        run: |
          xvfb-run -s "-screen 0 1280x1024x24" pytest -q gui/tests

  build-and-test:
    name: Build (no tests)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    needs: gui-tests
    env:
      CI_BIND_ADDRESS: 127.0.0.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.nuget/packages
            ~/.local/share/NuGet/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj','**/global.json') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore NuGet (with retries)
        shell: pwsh
        run: |
          # Retry wrapper for transient NuGet failures (3 attempts with exponential backoff)
          $maxAttempts = 3
          $baseDelay = 5
          $success = $false
          for ($i = 1; $i -le $maxAttempts; $i++) {
            Write-Host "dotnet restore attempt $i of $maxAttempts"
            dotnet restore USBDeviceManager.sln --verbosity minimal
            if ($LASTEXITCODE -eq 0) {
              $success = $true
              break
            }
            if ($i -lt $maxAttempts) {
              $delay = $baseDelay * [math]::Pow(2, $i - 1)
              Write-Host "Restore failed; sleeping $delay seconds before retrying..."
              Start-Sleep -Seconds $delay
            }
          }
          if (-not $success) {
            Write-Error "dotnet restore failed after $maxAttempts attempts"
            exit 1
          }

      - name: Publish long-run test helper
        shell: pwsh
        run: |
          if (Test-Path test-helpers/LongRunner) {
            dotnet publish test-helpers/LongRunner/LongRunner.csproj -c Release -o ./artifacts/longrunner
            Write-Host 'Published LongRunner to ./artifacts/longrunner'
          } else {
            Write-Host 'No long-run helper found; continuing without it.'
          }

      - name: Build
        run: dotnet build USBDeviceManager.sln --configuration Release --no-restore

  tests:
    name: Run Tests and Upload Results
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    needs: [gui-tests, build-and-test]
    env:
      CI_BIND_ADDRESS: 127.0.0.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.nuget/packages
            ~/.local/share/NuGet/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj','**/global.json') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore NuGet (with retries)
        shell: pwsh
        run: |
          $maxAttempts = 3
          $baseDelay = 5
          $success = $false
          for ($i = 1; $i -le $maxAttempts; $i++) {
            Write-Host "dotnet restore attempt $i of $maxAttempts"
            dotnet restore USBDeviceManager.sln --verbosity minimal
            if ($LASTEXITCODE -eq 0) {
              $success = $true
              break
            }
            if ($i -lt $maxAttempts) {
              $delay = $baseDelay * [math]::Pow(2, $i - 1)
              Write-Host "Restore failed; sleeping $delay seconds before retrying..."
              Start-Sleep -Seconds $delay
            }
          }
          if (-not $success) {
            Write-Error "dotnet restore failed after $maxAttempts attempts"
            exit 1
          }

      - name: Publish long-run test helper
        shell: pwsh
        run: |
          if (Test-Path test-helpers/LongRunner) {
            dotnet publish test-helpers/LongRunner/LongRunner.csproj -c Release -o ./artifacts/longrunner
            Write-Host 'Published LongRunner to ./artifacts/longrunner'
          } else {
            Write-Host 'No long-run helper found; continuing without it.'
          }

      - name: Detect long-sleep command for ShellRunner tests
        run: |
          echo "Detecting long-sleep helper for ShellRunner tests"

      - name: Set SHELLRUNNER_LONG_CMD (Linux)
        if: runner.os == 'Linux' || runner.os == 'Ubuntu' || runner.os == 'ubuntu-latest'
        shell: bash
        run: |
          if [ -f ./artifacts/longrunner/LongRunner.dll ]; then
            echo "SHELLRUNNER_LONG_CMD=dotnet|./artifacts/longrunner/LongRunner.dll --duration=30" >> $GITHUB_ENV
            echo "Set SHELLRUNNER_LONG_CMD to LongRunner via dotnet"
          elif [ -x /bin/bash ] && /bin/bash -c "sleep 0" >/dev/null 2>&1; then
            echo "SHELLRUNNER_LONG_CMD=/bin/bash|-c \"sleep 30\"" >> $GITHUB_ENV
            echo "Set SHELLRUNNER_LONG_CMD for bash"
          elif command -v sleep >/dev/null 2>&1; then
            echo "SHELLRUNNER_LONG_CMD=/bin/sh|-c \"sleep 30\"" >> $GITHUB_ENV
            echo "Set SHELLRUNNER_LONG_CMD for sh"
          else
            echo "No sleep command detected; leaving SHELLRUNNER_LONG_CMD unset"
          fi

      - name: Set SHELLRUNNER_LONG_CMD (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $helperDll = Join-Path $env:GITHUB_WORKSPACE 'artifacts/longrunner/LongRunner.dll'
          if (Test-Path $helperDll) {
            "SHELLRUNNER_LONG_CMD=dotnet|$helperDll --duration=30" | Out-File -FilePath $env:GITHUB_ENV -Encoding ASCII -Append
            Write-Host "Set SHELLRUNNER_LONG_CMD to LongRunner via dotnet"
          }
          } else {
            Write-Host "trx2html not available; copying .trx files to artifacts/trx for upload"
            Get-ChildItem -Path . -Filter *.trx -Recurse | ForEach-Object {
              $src = $_.FullName
              $destDir = Join-Path $PWD 'artifacts\trx'
              $dest = Join-Path $destDir $_.Name
              # Resolve canonical provider paths when possible
              try {
                $srcFull = (Resolve-Path -LiteralPath $src -ErrorAction Stop).ProviderPath
              } catch {
                $srcFull = $src
              }
              try {
                $destFull = (Resolve-Path -LiteralPath $dest -ErrorAction Stop).ProviderPath
              } catch {
                $destFull = $dest
              }
              # If source and destination are identical or source is already inside artifacts\trx, skip copying
              if ($srcFull -eq $destFull -or $srcFull.StartsWith($destDir, [System.StringComparison]::OrdinalIgnoreCase)) {
                Write-Host "Skipping copy: source already in artifacts/trx: $src"
                continue
              }
              try {
                Copy-Item -Path $src -Destination $dest -Force -ErrorAction Stop
              } catch {
                Write-Warning "Failed copying $src to $dest : $($_.Exception.Message)"
              }
            }
          }
              "SHELLRUNNER_LONG_CMD=pwsh|-Command Start-Sleep -Seconds 30" | Out-File -FilePath $env:GITHUB_ENV -Encoding ASCII -Append
              Write-Host "Set SHELLRUNNER_LONG_CMD for pwsh"
            }
            else {
              Write-Host "No PowerShell found; leaving SHELLRUNNER_LONG_CMD unset"
            }
          }

      - name: Enable in-process smoke server for CI (cross-platform)
        run: |
          echo "SMOKE_USE_TESTSERVER=1" >> $GITHUB_ENV

      - name: Ensure server stopped (optional auto-kill)
        shell: pwsh
        run: |
          if (Test-Path .\scripts\ensure-server-stopped.ps1) {
            Write-Host 'Running ensure-server-stopped.ps1 (respects AUTO_KILL env var).'
            $pwshPath = (Get-Command pwsh -ErrorAction SilentlyContinue)?.Source ?? 'pwsh'
            $args = @('-NoProfile','-NonInteractive','-ExecutionPolicy','Bypass','-File', "$PWD\scripts\ensure-server-stopped.ps1")
            $proc = Start-Process -FilePath $pwshPath -ArgumentList $args -NoNewWindow -PassThru -Wait
            $rc = $proc.ExitCode
            if ($rc -eq 2) {
              Write-Host "ensure-server-stopped returned 2 (AUTO_KILL=false and process present). Continuing as configured."
            } elseif ($rc -ne 0) {
              throw "ensure-server-stopped.ps1 failed with exit code $rc"
            }
          } else {
            Write-Host 'No ensure-server-stopped.ps1 present; skipping.'
          }

      - name: Build
        run: dotnet build USBDeviceManager.sln --configuration Release --no-restore

      - name: Run Unit tests (produce TRX)
        run: |
          dotnet test USBDeviceManager.sln --configuration Debug --filter "Category=Unit" --logger "trx;LogFileName=dotnet-tests-${{ matrix.os }}.trx"

      - name: Analyze tests results (per-matrix run)
        if: always()
        id: perjob-summary
        shell: bash
        run: |
          # Ensure Python available
          python3 --version || true
          set -euo pipefail
          # parse any TRX files produced in this job
          python3 scripts/ci-analyze.py || true
          # upload produced summaries for aggregation
          if [ -f tests-summary.json ]; then
            echo "Uploading per-job tests-summary artifacts"
            mkdir -p artifacts/summaries/tests
            cp tests-summary.json tests-summary.txt artifacts/summaries/tests/ || true
          fi

      - name: Convert TRX to HTML (Linux)
        if: runner.os == 'Linux'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Generate TRX HTML (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/trx-html artifacts/trx
          shopt -s globstar || true
          if command -v trx2html >/dev/null 2>&1; then
            for f in **/*.trx; do
              if [ -f "$f" ]; then
                trx2html "$f" -o "artifacts/trx-html/$(basename "$f").html" || true
              fi
            done
          else
            echo "trx2html not available; copying .trx files to artifacts/trx for upload"
            for f in **/*.trx; do
              if [ -f "$f" ]; then
                cp "$f" "artifacts/trx/$(basename "$f")"
              fi
            done
          fi

      - name: Convert TRX to HTML (Windows)
        if: runner.os == 'Windows'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Generate TRX HTML (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path artifacts\trx-html, artifacts\trx | Out-Null
          if (Get-Command trx2html -ErrorAction SilentlyContinue) {
            Get-ChildItem -Path . -Filter *.trx -Recurse | ForEach-Object {
              $out = Join-Path artifacts\trx-html ($_.Name + '.html')
              trx2html $_.FullName -o $out || Write-Host "trx2html failed for $($_.FullName)"
            }
          } else {
            Write-Host "trx2html not available; copying .trx files to artifacts\trx for upload"
            # Ensure we don't try to copy a file onto itself (causes failure)
            $destDir = (Get-Item -Path "artifacts\trx").FullName
            Get-ChildItem -Path . -Filter *.trx -Recurse | ForEach-Object {
              $dest = Join-Path $destDir $_.Name
              if ($_.FullName -ne $dest) {
                try {
                  Copy-Item $_.FullName -Destination $dest -Force -ErrorAction Stop
                } catch {
                  Write-Host "Failed copying $($_.FullName) -> $dest: $($_.Exception.Message)"
                }
              } else {
                Write-Host "Skipping copy of $($_.FullName) - destination equals source"
              }
            }
          }

      - name: Enrich tests logs (on failures/errors)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f tests-summary.json ]; then
            python3 scripts/enrich-summary.py --src tests-summary.json --out-dir artifacts/enriched/tests --report tests-enriched-report.txt || true
          else
            echo "No tests-summary.json present; skipping enrichment"
          fi

      - name: Produce prefixed tests errors file
        if: always()
        shell: bash
        env:
          PREFIX: ${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/enriched/$PREFIX
          # concise error snippet for faster triage
          grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,400p' > artifacts/enriched/$PREFIX/tests-enriched-errors.txt || true
          # preserve legacy enriched report copy for correlation
          if [ -f tests-enriched-report.txt ]; then cp tests-enriched-report.txt artifacts/enriched/$PREFIX/tests-enriched-report.txt || true; fi

      - name: Upload tests enriched errors (prefixed)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: enriched-errors-${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
          path: artifacts/enriched/${{ github.run_id }}-${{ github.job }}-${{ runner.os }}/tests-enriched-errors.txt

      - name: Upload tests enriched report (attempt 1)
        id: upload_tests_enriched_1
        if: always()
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: tests-enriched-report.txt

      - name: Upload tests enriched report (attempt 2)
        id: upload_tests_enriched_2
        if: steps.upload_tests_enriched_1.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: tests-enriched-report.txt

      - name: Upload tests enriched report (attempt 3)
        if: steps.upload_tests_enriched_1.outcome != 'success' && steps.upload_tests_enriched_2.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: tests-enriched-report.txt

      - name: Extract failure artifacts (on failure)
        if: failure()
        shell: pwsh
        run: |
          Write-Host "Running failure extractor for run $env:GITHUB_RUN_ID"
          pwsh -NoProfile -ExecutionPolicy Bypass -File .\scripts\extract-failures.ps1 -RunId $env:GITHUB_RUN_ID

      - name: Upload extracted failure artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: failure-artifacts-${{ github.run_id }}-${{ runner.os }}
          path: artifacts/enriched/failures/${{ github.run_id }}

  unit-tests:
    name: Unit Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]
    needs: build-and-test
    env:
      CI_BIND_ADDRESS: 127.0.0.1
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Cache NuGet packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.nuget/packages
            ~/.local/share/NuGet/packages
          key: nuget-${{ runner.os }}-${{ hashFiles('**/*.csproj','**/global.json') }}
          restore-keys: |
            nuget-${{ runner.os }}-

      - name: Restore NuGet (with retries)
        shell: pwsh
        run: |
          $maxAttempts = 3
          $baseDelay = 5
          $success = $false
          for ($i = 1; $i -le $maxAttempts; $i++) {
            Write-Host "dotnet restore attempt $i of $maxAttempts"
            dotnet restore USBDeviceManager.sln --verbosity minimal
            if ($LASTEXITCODE -eq 0) {
              $success = $true
              break
            }
            if ($i -lt $maxAttempts) {
              $delay = $baseDelay * [math]::Pow(2, $i - 1)
              Write-Host "Restore failed; sleeping $delay seconds before retrying..."
              Start-Sleep -Seconds $delay
            }
          }
          if (-not $success) {
            Write-Error "dotnet restore failed after $maxAttempts attempts"
            exit 1
          }

      - name: Build
        run: dotnet build USBDeviceManager.sln --configuration Release --no-restore

      - name: Run Unit tests (produce TRX)
        run: |
          dotnet test USBDeviceManager.sln --configuration Debug --filter "Category=Unit" --logger "trx;LogFileName=dotnet-unit-tests-${{ matrix.os }}.trx"

      - name: Convert TRX to HTML (Linux)
        if: runner.os == 'Linux'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Generate TRX HTML (Linux)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          npm install -g trx2html || true
          mkdir -p artifacts/trx-html
          shopt -s globstar || true
          for f in **/*.trx; do
            if [ -f "$f" ]; then
              trx2html "$f" -o "artifacts/trx-html/$(basename "$f").html" || true
            fi
          done

      - name: Convert TRX to HTML (Windows)
        if: runner.os == 'Windows'
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Generate TRX HTML (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          # Do not attempt to install missing npm package (can 404); prefer a safe check
          New-Item -ItemType Directory -Force -Path artifacts\trx-html, artifacts\trx | Out-Null
          if (Get-Command trx2html -ErrorAction SilentlyContinue) {
            Get-ChildItem -Path . -Filter *.trx -Recurse | ForEach-Object {
              $out = Join-Path artifacts\trx-html ($_.Name + '.html')
              try {
                trx2html $_.FullName -o $out
              } catch {
                Write-Host "trx2html failed for $($_.FullName): $($_.Exception.Message)"
              }
            }
          } else {
            Write-Host "trx2html not available; copying .trx files to artifacts\trx for upload"
            Get-ChildItem -Path . -Filter *.trx -Recurse | ForEach-Object {
              Copy-Item $_.FullName -Destination (Join-Path artifacts\trx $_.Name) -Force
            }
          }

      - name: Analyze unit test results (per-job)
        if: always()
        id: perunit-summary
        shell: bash
        run: |
          python3 --version || true
          set -euo pipefail
          python3 scripts/ci-analyze.py || true
          if [ -f tests-summary.json ]; then
            mkdir -p artifacts/summaries/unit
            cp tests-summary.json tests-summary.txt artifacts/summaries/unit/ || true
          fi
      - name: Enrich unit logs (on failures/errors)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f tests-summary.json ]; then
            python3 scripts/enrich-summary.py --src tests-summary.json --out-dir artifacts/enriched/unit --report unit-tests-enriched-report.txt || true
          else
            echo "No tests-summary.json present; skipping unit enrichment"
          fi

      - name: Produce prefixed unit errors file
        if: always()
        shell: bash
        env:
          PREFIX: ${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/enriched/$PREFIX
          grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,400p' > artifacts/enriched/$PREFIX/unit-tests-enriched-errors.txt || true
          if [ -f unit-tests-enriched-report.txt ]; then cp unit-tests-enriched-report.txt artifacts/enriched/$PREFIX/unit-tests-enriched-report.txt || true; fi

      - name: Upload unit enriched errors (prefixed)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-enriched-errors-${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
          path: artifacts/enriched/${{ github.run_id }}-${{ github.job }}-${{ runner.os }}/unit-tests-enriched-errors.txt
      - name: Upload unit enriched report (attempt 1)
        id: upload_unit_enriched_1
        if: always()
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt

      - name: Upload unit enriched report (attempt 2)
        id: upload_unit_enriched_2
        if: steps.upload_unit_enriched_1.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt

      - name: Upload unit enriched report (attempt 3)
        if: steps.upload_unit_enriched_1.outcome != 'success' && steps.upload_unit_enriched_2.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt
      - name: Upload unit test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-unit-tests-${{ matrix.os }}
          path: |
            '**/*dotnet-unit-tests-${{ matrix.os }}.trx'
            '**/*dotnet-tests.local.trx'
            '**/TestResults/*.trx'
            '**/*/TestResults/*.trx'

      - name: Run isolated AgentHeartbeat tests
        shell: pwsh
        run: |
          if (Test-Path server/AgentHeartbeat.Tests) {
            dotnet test server/AgentHeartbeat.Tests/AgentHeartbeat.Tests.csproj --configuration Debug --logger "trx;LogFileName=agentheartbeat-${{ matrix.os }}.trx"
          } else {
            Write-Host 'No isolated AgentHeartbeat.Tests project found; skipping.'
          }

      - name: Upload dotnet test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-tests-${{ matrix.os }}
          path: |
            '**/*dotnet-tests-${{ matrix.os }}.trx'
            '**/*dotnet-tests.local.trx'
            '**/TestResults/*.trx'
            '**/*/TestResults/*.trx'
            server/AgentHeartbeat.Tests/**/TestResults/*.trx
            server/**/TestResults/*.trx

      - name: Run sanitize-repo check (PowerShell script)
        shell: pwsh
        run: |
          if (Test-Path .\scripts\sanitize-repo.ps1) { .\scripts\sanitize-repo.ps1 -CheckOnly } else { Write-Host 'No sanitize script present, skipping.' }

      - name: Run PowerShell agent tests (Windows only)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          if (Test-Path .\ci\run-tests.ps1) {
            .\ci\run-tests.ps1 -RunAgentTests -RunServerTests:$false -NonInteractive
          } elseif (Test-Path .\agent\SimRacingAgent.Tests) {
            Import-Module Pester -Force -MinimumVersion 5.0
            Invoke-Pester -Script @{ Path = '.\agent\SimRacingAgent.Tests'; OutputFormat = 'NUnitXml'; OutputFile = 'agent-pester-results.xml' }
            Write-Host 'Pester results: agent-pester-results.xml'
          } else {
            Write-Host 'No agent tests found; skipping.'
          }

      - name: Run Master TestRunner (PowerShell, cross-platform)
        shell: pwsh
        run: |
          if (Test-Path ./agent/SimRacingAgent.Tests/TestRunner.ps1) {
            Write-Host 'Invoking master TestRunner (agent + server focused runners)...'
            pwsh -NoProfile -ExecutionPolicy Bypass -File ./agent/SimRacingAgent.Tests/TestRunner.ps1
          } else {
            Write-Host 'No master TestRunner found; skipping.'
          }

      - name: Upload agent test results (Windows only)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: agent-tests-windows
          path: |
            agent-pester-results.xml
            **/TestResults/*.trx
            agent/SimRacingAgent.Tests/Integration/logs/**
            server/USBDeviceManager.Tests/Integration/logs/**
            agent/SimRacingAgent.Tests/Functional/logs/**
            agent/SimRacingAgent.Tests/logs/**

      - name: Upload server logs (build matrix)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: server-log-${{ matrix.os }}
          path: |
            scripts/tmp/server.log
            scripts/tmp/server.err.log
            scripts/tmp/ensure-stopped.log
  e2e-windows:
    runs-on: windows-latest
    needs: unit-tests
    if: always()
    steps:
      - name: Set TEST_PORT (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $listener = [System.Net.Sockets.TcpListener]::new([System.Net.IPAddress]::Loopback,0)
          $listener.Start()
          $port = ($listener.LocalEndpoint -as [System.Net.IPEndPoint]).Port
          $listener.Stop()
          "TEST_PORT=$port" | Out-File -FilePath $env:GITHUB_ENV -Encoding ASCII -Append
          Write-Host "Set TEST_PORT=$port"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore USBDeviceManager.sln

      - name: Restore AgentE2E project
        run: dotnet restore server/AgentE2E.Tests/AgentE2E.Tests.csproj

      - name: Build
        run: dotnet build server/AgentE2E.Tests/AgentE2E.Tests.csproj --configuration Release --no-restore

      - name: Run Agent E2E tests (Windows)
        run: |
          dotnet test server/AgentE2E.Tests/AgentE2E.Tests.csproj --configuration Debug --logger "trx;LogFileName=AgentE2E.windows.trx"

      - name: Upload E2E results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: agent-e2e-windows
          path: server/AgentE2E.Tests/**/TestResults/*.trx

      - name: Analyze E2E results (per-job)
        if: always()
        shell: bash
        run: |
          python3 --version || true
          set -euo pipefail
          python3 scripts/ci-analyze.py || true
          if [ -f tests-summary.json ]; then
            mkdir -p artifacts/summaries/e2e
            cp tests-summary.json tests-summary.txt artifacts/summaries/e2e/ || true
          fi

      - name: Enrich E2E logs (on failures/errors)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f tests-summary.json ]; then
            python3 scripts/enrich-summary.py --src tests-summary.json --out-dir artifacts/enriched/e2e --report e2e-enriched-report.txt || true
          else
            echo "No tests-summary.json present; skipping e2e enrichment"
          fi

      - name: Produce prefixed e2e errors file
        if: always()
        shell: bash
        env:
          PREFIX: ${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/enriched/$PREFIX
          grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,400p' > artifacts/enriched/$PREFIX/e2e-enriched-errors.txt || true
          if [ -f e2e-enriched-report.txt ]; then cp e2e-enriched-report.txt artifacts/enriched/$PREFIX/e2e-enriched-report.txt || true; fi

      - name: Upload e2e enriched errors (prefixed)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-enriched-errors-${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
          path: artifacts/enriched/${{ github.run_id }}-${{ github.job }}-${{ runner.os }}/e2e-enriched-errors.txt

      - name: Upload e2e enriched report (attempt 1)
        id: upload_e2e_enriched_1
        if: always()
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

      - name: Upload e2e enriched report (attempt 2)
        id: upload_e2e_enriched_2
        if: steps.upload_e2e_enriched_1.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

      - name: Upload e2e enriched report (attempt 3)
        if: steps.upload_e2e_enriched_1.outcome != 'success' && steps.upload_e2e_enriched_2.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

      # per-job E2E analysis removed; regression analysis handled by analyze-regression job

  integration-tests:
    runs-on: windows-latest
    needs: unit-tests
    if: always()
    steps:
      - name: Set TEST_PORT (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $listener = [System.Net.Sockets.TcpListener]::new([System.Net.IPAddress]::Loopback,0)
          $listener.Start()
          $port = ($listener.LocalEndpoint -as [System.Net.IPEndPoint]).Port
          $listener.Stop()
          "TEST_PORT=$port" | Out-File -FilePath $env:GITHUB_ENV -Encoding ASCII -Append
          Write-Host "Set TEST_PORT=$port"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Start server for Integration tests (PowerShell)
        if: always()
        shell: pwsh
        run: |
          if (Test-Path ./scripts/start-server-and-wait.ps1) { .\scripts\start-server-and-wait.ps1 -NonInteractive -Port $Env:TEST_PORT } else { Write-Host 'No orchestration script present, skipping server start.' }

      - name: Run Integration tests
        shell: pwsh
        run: |
          dotnet test USBDeviceManager.sln --configuration Debug --filter "Category=Integration" --logger "trx;LogFileName=integration-${{ runner.os }}.trx"

      - name: Stop test environment (PowerShell)
        if: always()
        shell: pwsh
        run: |
          if (Test-Path ./scripts/stop-test-environment.ps1) { .\scripts\stop-test-environment.ps1 } else { Write-Host 'No stop script present.' }

      - name: Upload server logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: server-log-${{ runner.os }}
          path: |
            scripts/tmp/server.log
            scripts/tmp/server.err.log
            scripts/tmp/ensure-stopped.log

      - name: Upload integration results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-tests-${{ runner.os }}
          path: '**/*integration-${{ runner.os }}.trx'

      - name: Analyze integration results (per-job)
        if: always()
        id: per-integration-summary
        shell: bash
        run: |
          python3 --version || true
          set -euo pipefail
          python3 scripts/ci-analyze.py || true
          if [ -f tests-summary.json ]; then
            mkdir -p artifacts/summaries/integration
            cp tests-summary.json tests-summary.txt artifacts/summaries/integration/ || true
          fi
      - name: Enrich integration logs (on failures/errors)
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          if [ -f tests-summary.json ]; then
            python3 scripts/enrich-summary.py --src tests-summary.json --out-dir artifacts/enriched/integration --report integration-enriched-report.txt || true
          else
            echo "No tests-summary.json present; skipping integration enrichment"
          fi

      - name: Produce prefixed integration errors file
        if: always()
        shell: bash
        env:
          PREFIX: ${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/enriched/$PREFIX
          grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,400p' > artifacts/enriched/$PREFIX/integration-enriched-errors.txt || true
          if [ -f integration-enriched-report.txt ]; then cp integration-enriched-report.txt artifacts/enriched/$PREFIX/integration-enriched-report.txt || true; fi

      - name: Upload integration enriched errors (prefixed)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-enriched-errors-${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
          path: artifacts/enriched/${{ github.run_id }}-${{ github.job }}-${{ runner.os }}/integration-enriched-errors.txt
      - name: Upload integration enriched report (attempt 1)
        id: upload_integration_enriched_1
        if: always()
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

      - name: Upload integration enriched report (attempt 2)
        id: upload_integration_enriched_2
        if: steps.upload_integration_enriched_1.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

      - name: Upload integration enriched report (attempt 3)
        if: steps.upload_integration_enriched_1.outcome != 'success' && steps.upload_integration_enriched_2.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

  analyze-results:
    name: Analyze Test Results
    runs-on: ubuntu-latest
    if: always()
    needs: tests
    outputs:
      tests_ok: ${{ steps.summary.outputs.tests_ok }}
      failed_count: ${{ steps.summary.outputs.failed_count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download dotnet test artifacts (Ubuntu)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-tests-ubuntu-latest
          path: artifacts/tests/ubuntu
        continue-on-error: true

      - name: Download unit test artifacts (Ubuntu)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-unit-tests-ubuntu-latest
          path: artifacts/unit/ubuntu
        continue-on-error: true

      - name: Download dotnet test artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-tests-windows-latest
          path: artifacts/tests/windows
        continue-on-error: true

      - name: Download unit test artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-unit-tests-windows-latest
          path: artifacts/unit/windows
        continue-on-error: true

      - name: Download agent & integration artifacts (if present)
        uses: actions/download-artifact@v4
        with:
          name: agent-tests-windows
          path: artifacts/agent
        continue-on-error: true

      - name: Generate test summary (parse TRX)
        id: summary
        shell: bash
        run: |
          # Ensure a known Python is available for the analyzer
          python3 --version || true

          set -euo pipefail
          echo "Searching for TRX files under $(pwd)"
          mapfile -t files < <(find . -type f -name '*.trx' || true)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No TRX files found; setting outputs and exiting"
            echo "tests_ok=false" >> $GITHUB_OUTPUT
            echo "failed_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi
            python3 scripts/ci-analyze.py
          failed=$(python3 -c "import json; print(json.load(open('tests-summary.json'))['failed'])")
          if [ "$failed" -eq 0 ]; then
            echo "tests_ok=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ok=false" >> $GITHUB_OUTPUT
          fi
          echo "failed_count=$failed" >> $GITHUB_OUTPUT

      - name: Upload test summary artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: tests-summary
          path: |
            tests-summary.json
            tests-summary.txt

      - name: Run log analysis and enrich report (on failures)
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        run: |
          set -euo pipefail
          echo "Failures detected, gathering logs and generating enriched report"
          mkdir -p artifacts/enriched/tests
          echo "=== Enriched Test Report ===" > tests-enriched-report.txt
          echo "See tests-summary.json for full summary." >> tests-enriched-report.txt
          echo "\n--- Failures ---\n" >> tests-enriched-report.txt
          cat tests-summary.txt >> tests-enriched-report.txt

          if command -v jq >/dev/null 2>&1; then
            jq -c '.failures[]' tests-summary.json | while read -r fail; do
              name=$(echo "$fail" | jq -r '.name' | sed 's/[^a-zA-Z0-9._-]/_/g' | cut -c1-200)
              msg=$(echo "$fail" | jq -r '.message')
              fileloc=$(echo "$fail" | jq -r '.file')
              outdir="artifacts/enriched/tests/$name"
              mkdir -p "$outdir"
              echo "Name: $name" > "$outdir/details.txt"
              echo "Message:" >> "$outdir/details.txt"
              echo "$msg" >> "$outdir/details.txt"
              echo "Source file: $fileloc" >> "$outdir/details.txt"
              if [ -f "$fileloc" ]; then
                cp "$fileloc" "$outdir/" || true
              fi
              grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,200p' > "$outdir/log-excerpts.txt" || true
            done
          else
            echo "jq not available; skipping per-failure folder creation" >> tests-enriched-report.txt
          fi

          echo "Wrote tests-enriched-report.txt and per-failure folders"

      - name: Upload enriched report (failures)
        if: steps.summary.outputs.failed_count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: tests-enriched-report.txt

  analyze-regression:
    name: Analyze Regression Results
    runs-on: ubuntu-latest
    if: always()
    needs: integration-tests
    outputs:
      tests_ok: ${{ steps.summary.outputs.tests_ok }}
      failed_count: ${{ steps.summary.outputs.failed_count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download integration artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: integration-tests-windows
          path: artifacts/regression/windows
        continue-on-error: true

      - name: Generate regression summary (parse TRX)
        id: summary
        shell: bash
        run: |
          # Ensure a known Python is available for the analyzer
          python3 --version || true

          set -euo pipefail
          python3 scripts/ci-analyze.py
          failed=$(python3 -c "import json; print(json.load(open('tests-summary.json'))['failed'])")
          if [ "$failed" -eq 0 ]; then
            echo "tests_ok=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ok=false" >> $GITHUB_OUTPUT
          fi
          echo "failed_count=$failed" >> $GITHUB_OUTPUT

      - name: Upload regression summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: regression-tests-summary
          path: |
            tests-summary.json
            tests-summary.txt

      - name: Enrich regression report (on failures)
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        run: |
          set -euo pipefail
          echo "Regression failures detected; generating enriched regression report"
          mkdir -p artifacts/enriched/regression
          echo "=== Regression Enriched Report ===" > regression-enriched-report.txt
          cat tests-summary.txt >> regression-enriched-report.txt
          if command -v jq >/dev/null 2>&1; then
            jq -c '.failures[]' tests-summary.json | while read -r fail; do
              name=$(echo "$fail" | jq -r '.name' | sed 's/[^a-zA-Z0-9._-]/_/g' | cut -c1-200)
              msg=$(echo "$fail" | jq -r '.message')
              fileloc=$(echo "$fail" | jq -r '.file')
              outdir="artifacts/enriched/regression/$name"
              mkdir -p "$outdir"
              echo "Name: $name" > "$outdir/details.txt"
              echo "$msg" >> "$outdir/details.txt"
              echo "Source file: $fileloc" >> "$outdir/details.txt"
              if [ -f "$fileloc" ]; then cp "$fileloc" "$outdir/" || true; fi
              grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,200p' > "$outdir/log-excerpts.txt" || true
            done
          else
            grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion" . | sed -n '1,200p' >> regression-enriched-report.txt || true
          fi
      - name: Produce prefixed regression errors file
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        env:
          PREFIX: ${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
        run: |
          set -euo pipefail
          mkdir -p artifacts/enriched/$PREFIX
          grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,400p' > artifacts/enriched/$PREFIX/regression-enriched-errors.txt || true
          if [ -f regression-enriched-report.txt ]; then cp regression-enriched-report.txt artifacts/enriched/$PREFIX/regression-enriched-report.txt || true; fi

      - name: Upload regression enriched errors (prefixed)
        if: steps.summary.outputs.failed_count != '0'
        uses: actions/upload-artifact@v4
        with:
          name: regression-enriched-errors-${{ github.run_id }}-${{ github.job }}-${{ runner.os }}
          path: artifacts/enriched/${{ github.run_id }}-${{ github.job }}-${{ runner.os }}/regression-enriched-errors.txt

  analyze-integration:
    name: Analyze Integration Results
    runs-on: ubuntu-latest
    if: always()
    needs: integration-tests
    outputs:
      tests_ok: ${{ steps.summary.outputs.tests_ok }}
      failed_count: ${{ steps.summary.outputs.failed_count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download integration artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: integration-tests-windows
          path: artifacts/integration/windows
        continue-on-error: true

      - name: Generate integration summary (parse TRX)
        id: summary
        shell: bash
        run: |
          # Ensure a known Python is available for the analyzer
          python3 --version || true

          set -euo pipefail
          python3 scripts/ci-analyze.py
          failed=$(python3 -c "import json; print(json.load(open('tests-summary.json'))['failed'])")
          if [ "$failed" -eq 0 ]; then
            echo "tests_ok=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ok=false" >> $GITHUB_OUTPUT
          fi
          echo "failed_count=$failed" >> $GITHUB_OUTPUT

      - name: Upload integration summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-tests-summary
          path: |
            tests-summary.json
            tests-summary.txt

      - name: Enrich integration report (on failures)
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        run: |
          set -euo pipefail
          echo "Integration failures detected; generating enriched integration report"
          mkdir -p artifacts/enriched/integration
          echo "=== Integration Enriched Report ===" > integration-enriched-report.txt
          cat tests-summary.txt >> integration-enriched-report.txt
          if command -v jq >/dev/null 2>&1; then
            jq -c '.failures[]' tests-summary.json | while read -r fail; do
              name=$(echo "$fail" | jq -r '.name' | sed 's/[^a-zA-Z0-9._-]/_/g' | cut -c1-200)
              msg=$(echo "$fail" | jq -r '.message')
              fileloc=$(echo "$fail" | jq -r '.file')
              outdir="artifacts/enriched/integration/$name"
              mkdir -p "$outdir"
              echo "Name: $name" > "$outdir/details.txt"
              echo "$msg" >> "$outdir/details.txt"
              echo "Source file: $fileloc" >> "$outdir/details.txt"
              if [ -f "$fileloc" ]; then cp "$fileloc" "$outdir/" || true; fi
              grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,200p' > "$outdir/log-excerpts.txt" || true
            done
          else
            grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion" . | sed -n '1,200p' >> integration-enriched-report.txt || true
          fi

      - name: Upload integration enriched report (attempt 1)
        id: upload_integration_enriched_1b
        if: steps.summary.outputs.failed_count != '0'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

      - name: Upload integration enriched report (attempt 2)
        id: upload_integration_enriched_2b
        if: steps.upload_integration_enriched_1b.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

      - name: Upload integration enriched report (attempt 3)
        if: steps.upload_integration_enriched_1b.outcome != 'success' && steps.upload_integration_enriched_2b.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: integration-enriched-report-${{ github.job }}-${{ runner.os }}
          path: integration-enriched-report.txt

  analyze-e2e:
    name: Analyze E2E Results
    runs-on: ubuntu-latest
    if: always()
    needs: e2e-windows
    outputs:
      tests_ok: ${{ steps.summary.outputs.tests_ok }}
      failed_count: ${{ steps.summary.outputs.failed_count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download E2E artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: agent-e2e-windows
          path: artifacts/e2e/windows
        continue-on-error: true

      - name: Generate E2E summary (parse TRX)
        id: summary
        shell: bash
        run: |
          # Ensure a known Python is available for the analyzer
          python3 --version || true

          set -euo pipefail
          mapfile -t files < <(find . -type f -name '*.trx' || true)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No TRX files found; setting outputs and exiting"
            echo "tests_ok=false" >> $GITHUB_OUTPUT
            echo "failed_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          python3 scripts/ci-analyze.py
          failed=$(python3 -c "import json; print(json.load(open('tests-summary.json'))['failed'])")
          if [ "$failed" -eq 0 ]; then
            echo "tests_ok=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ok=false" >> $GITHUB_OUTPUT
          fi
          echo "failed_count=$failed" >> $GITHUB_OUTPUT

      - name: Upload e2e test summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: e2e-tests-summary
          path: |
            tests-summary.json
            tests-summary.txt

      - name: Enrich e2e report (on failures)
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        run: |
          set -euo pipefail
          echo "E2E failures detected; generating enriched e2e report"
          mkdir -p artifacts/enriched/e2e
          echo "=== E2E Enriched Report ===" > e2e-enriched-report.txt
          cat tests-summary.txt >> e2e-enriched-report.txt
          if command -v jq >/dev/null 2>&1; then
            jq -c '.failures[]' tests-summary.json | while read -r fail; do
              name=$(echo "$fail" | jq -r '.name' | sed 's/[^a-zA-Z0-9._-]/_/g' | cut -c1-200)
              msg=$(echo "$fail" | jq -r '.message')
              fileloc=$(echo "$fail" | jq -r '.file')
              outdir="artifacts/enriched/e2e/$name"
              mkdir -p "$outdir"
              echo "Name: $name" > "$outdir/details.txt"
              echo "$msg" >> "$outdir/details.txt"
              echo "Source file: $fileloc" >> "$outdir/details.txt"
              if [ -f "$fileloc" ]; then cp "$fileloc" "$outdir/" || true; fi
              grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,200p' > "$outdir/log-excerpts.txt" || true
            done
          else
            grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion" . | sed -n '1,200p' >> e2e-enriched-report.txt || true
          fi

      - name: Upload e2e enriched report (attempt 1)
        id: upload_e2e_enriched_1b
        if: steps.summary.outputs.failed_count != '0'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

      - name: Upload e2e enriched report (attempt 2)
        id: upload_e2e_enriched_2b
        if: steps.upload_e2e_enriched_1b.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

      - name: Upload e2e enriched report (attempt 3)
        if: steps.upload_e2e_enriched_1b.outcome != 'success' && steps.upload_e2e_enriched_2b.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: e2e-enriched-report-${{ github.job }}-${{ runner.os }}
          path: e2e-enriched-report.txt

  final-report:
    name: CI Final Test Summary
    runs-on: ubuntu-latest
    env:
      HTML_COMMENT_MAX_CHARS: '60000'
    needs: [analyze-unit, analyze-results, analyze-regression, analyze-integration, analyze-e2e]
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts for aggregation
        uses: actions/download-artifact@v4
        with:
          path: artifacts/downloaded
        continue-on-error: true

      - name: Rehydrate downloaded artifacts into expected folders
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/summaries/unit artifacts/summaries/tests artifacts/summaries/e2e artifacts/summaries/integration \
                   artifacts/enriched/unit artifacts/enriched/tests artifacts/enriched/e2e artifacts/enriched/integration artifacts/enriched/regression artifacts/downloaded-raw
          for d in artifacts/downloaded/*; do
            [ -d "$d" ] || continue
            bn=$(basename "$d")
            # Summaries
            case "$bn" in
              tests-summary* ) cp -r "$d"/* artifacts/summaries/tests/ || true ;;
              unit-tests-summary* ) cp -r "$d"/* artifacts/summaries/unit/ || true ;;
              regression-tests-summary* ) cp -r "$d"/* artifacts/summaries/regression/ || true ;;
              integration-tests-summary* ) cp -r "$d"/* artifacts/summaries/integration/ || true ;;
              e2e-tests-summary* ) cp -r "$d"/* artifacts/summaries/e2e/ || true ;;
            esac
            # Enriched reports (different jobs may have suffixed names)
            if ls "$d"/*integration-enriched-report.txt >/dev/null 2>&1; then mkdir -p artifacts/enriched/integration; cp "$d"/*integration-enriched-report.txt artifacts/enriched/integration/ || true; fi
            if ls "$d"/*regression-enriched-report.txt >/dev/null 2>&1; then mkdir -p artifacts/enriched/regression; cp "$d"/*regression-enriched-report.txt artifacts/enriched/regression/ || true; fi
            if ls "$d"/*unit-tests-enriched-report.txt >/dev/null 2>&1; then mkdir -p artifacts/enriched/unit; cp "$d"/*unit-tests-enriched-report.txt artifacts/enriched/unit/ || true; fi
            if ls "$d"/*e2e-enriched-report.txt >/dev/null 2>&1; then mkdir -p artifacts/enriched/e2e; cp "$d"/*e2e-enriched-report.txt artifacts/enriched/e2e/ || true; fi
            if ls "$d"/*tests-enriched-report.txt >/dev/null 2>&1; then mkdir -p artifacts/enriched/tests; cp "$d"/*tests-enriched-report.txt artifacts/enriched/tests/ || true; fi
            # copy anything else for manual inspection
            cp -r "$d" artifacts/downloaded-raw/ || true
          done

      - name: Aggregate summaries into final report
        id: aggregate
        shell: bash
        run: |
          # Ensure a known Python is available for the analyzer
          python3 --version || true

          set -euo pipefail
          out=final-report.txt
          echo "CI Final Test Summary" > $out
          echo "Generated (UTC): $(date -u)" >> $out
          any_failed=false

          gather() {
            type=$1
            json="artifacts/summaries/${type}/tests-summary.json"
            txt="artifacts/summaries/${type}/tests-summary.txt"
            echo "\n--- ${type^^} ---\n" >> $out
            if [ -f "$json" ]; then
              # Use a small helper to print summary to avoid YAML parser problems
              python3 scripts/print-summary.py "$json" >> $out
              failed=$(python3 -c "import json;print(int(json.load(open('$json')).get('failed',0)))")
              if [ "$failed" -gt 0 ]; then any_failed=true; fi
              if [ -f "$txt" ]; then
                echo "\nSummary details:" >> $out
                sed -n '1,200p' "$txt" >> $out
              fi
            else
              echo "(no ${type} summary)" >> $out
            fi
          }

          gather unit
          gather tests
          gather e2e
          gather integration

          # Append any per-analyzer enriched reports if present
          if [ -f "artifacts/enriched/unit/unit-tests-enriched-report.txt" ]; then
            echo "\n--- UNIT ENRICHED REPORT ---\n" >> $out
            sed -n '1,400p' artifacts/enriched/unit/unit-tests-enriched-report.txt >> $out
          fi
          if [ -f "artifacts/enriched/regression/regression-enriched-report.txt" ]; then
            echo "\n--- REGRESSION ENRICHED REPORT ---\n" >> $out
            sed -n '1,400p' artifacts/enriched/regression/regression-enriched-report.txt >> $out
          fi
          if [ -f "artifacts/enriched/integration/integration-enriched-report.txt" ]; then
            echo "\n--- INTEGRATION ENRICHED REPORT ---\n" >> $out
            sed -n '1,400p' artifacts/enriched/integration/integration-enriched-report.txt >> $out
          fi
          if [ -f "artifacts/enriched/e2e/e2e-enriched-report.txt" ]; then
            echo "\n--- E2E ENRICHED REPORT ---\n" >> $out
            sed -n '1,400p' artifacts/enriched/e2e/e2e-enriched-report.txt >> $out
          fi
          if [ -f "artifacts/enriched/tests/tests-enriched-report.txt" ]; then
            echo "\n--- TESTS ENRICHED REPORT ---\n" >> $out
            sed -n '1,400p' artifacts/enriched/tests/tests-enriched-report.txt >> $out
          fi

          echo "Wrote $out"
          echo "any_failed=$any_failed" >> $GITHUB_OUTPUT

      - name: Generate HTML final report
        shell: bash
        run: |
          set -euo pipefail
          # renderer will search recursively under artifacts/summaries
          python3 scripts/generate-final-report-html.py --src artifacts/summaries --out final-report.html || python3 scripts/generate-final-report-html.py --out final-report.html

      - name: Copy final report to stable artifacts/latest-report
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p artifacts/latest-report
          cp -f final-report.html artifacts/latest-report/final-report.html || true
          # include any enriched folders so links in the HTML resolve when downloaded
          if [ -d artifacts/enriched ]; then
            rm -rf artifacts/latest-report/enriched || true
            cp -r artifacts/enriched artifacts/latest-report/enriched || true
          fi

      - name: Upload latest report artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: latest-test-report
          path: |
            artifacts/latest-report/final-report.html
            artifacts/latest-report/enriched/**

      - name: Upload final report HTML
        uses: actions/upload-artifact@v4
        with:
          name: final-test-report-html
          path: final-report.html

      - name: Upload final report
        uses: actions/upload-artifact@v4
        with:
          name: final-test-report
          path: final-report.txt

      - name: Post final report as PR comment (on failures)
        if: ${{ steps.aggregate.outputs.any_failed == 'true' && github.event.pull_request != null }}
        shell: bash
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          echo "Posting final report to PR #${PR_NUMBER}"
          body=$(python3 -c "import json,sys; print(json.dumps(open('final-report.txt',encoding='utf-8').read()))")
          curl -s -H "Authorization: Bearer $GITHUB_TOKEN" -H "Content-Type: application/json" -d "{\"body\":$body}" "https://api.github.com/repos/$REPO/issues/$PR_NUMBER/comments" > /dev/null
          echo "Comment posted"

      - name: Create check-run failure summary (on failures)
        if: ${{ steps.aggregate.outputs.any_failed == 'true' }}
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const body = fs.existsSync('final-report.txt') ? fs.readFileSync('final-report.txt','utf8') : '';
            const lines = body.split(/\r?\n/).slice(0,40).join('\n');
            const text = `\`\`\`\n${lines}\n\`\`\``;
            const output = {
              title: 'CI Failure Summary',
              summary: 'One or more test suites failed. See the full report artifact for details.',
              text: text
            };
            try {
              await github.rest.checks.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                name: 'CI Failure Summary',
                head_sha: context.sha,
                status: 'completed',
                conclusion: 'neutral',
                output: output
              });
            } catch (err) {
              if (err && err.status === 403) {
                console.log('Skipping check-run creation: Resource not accessible by integration (403)');
              } else {
                throw err;
              }
            }

      - name: Fail workflow if any tests failed
        if: ${{ steps.aggregate.outputs.any_failed == 'true' }}
        run: |
          echo "One or more test suites failed; marking workflow as failed"
          exit 1

      - name: Prepare failure-report PR files
        if: ${{ steps.aggregate.outputs.any_failed == 'true' }}
        shell: bash
        run: |
          set -euo pipefail
          sha=$(echo "${GITHUB_SHA}" | cut -c1-7)
          mkdir -p .github/ci-failures
          outf=".github/ci-failures/ci-failure-${sha}.md"
          echo "# CI Test Failures for ${GITHUB_SHA}" > "$outf"
          echo "" >> "$outf"
          echo 'Generated by CI. See the aggregated report attached.' >> "$outf"
          echo "" >> "$outf"
          echo '```' >> "$outf"
          sed -n '1,400p' final-report.txt >> "$outf" || true
          echo '```' >> "$outf"
          # also attach the generated HTML report if present
          if [ -f final-report.html ]; then
            cp -f final-report.html ".github/ci-failures/final-report-${sha}.html" || true
          fi

      - name: Create PR with failure report
        if: ${{ steps.aggregate.outputs.any_failed == 'true' }}
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "CI: test failures report (${GITHUB_SHA})"
          branch: "ci/failures-${{ github.sha }}"
          title: "CI: Test failures (${GITHUB_SHA})"
          body: |
            Automated PR containing the CI failure report generated by the workflow.

            - Aggregated text report: `.github/ci-failures/ci-failure-${{ github.sha }}.md`
            - HTML report (preview): `.github/ci-failures/final-report-${{ github.sha }}.html`

            You can also download artifacts for this workflow run (includes `latest-test-report`) here:
            ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

            Review the files in the PR to investigate failures.
          labels: ci-failure

      - name: Create gist of HTML report and comment on PR
        if: ${{ steps.aggregate.outputs.any_failed == 'true' }}
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const fullSha = process.env.GITHUB_SHA || '';
            const short = (fullSha || '').substring(0,7);
            const htmlPath = `.github/ci-failures/final-report-${short}.html`;
            try {
              if (!fs.existsSync(htmlPath)) {
                console.log('HTML report not found at', htmlPath);
                return;
              }
              const content = fs.readFileSync(htmlPath, 'utf8');
              const gist = await github.rest.gists.create({
                files: { [`final-report-${short}.html`]: { content } },
                public: false,
                description: `CI final-report HTML for ${fullSha}`
              });
              console.log('Created gist', gist.data.html_url);

              // find the PR by head branch
              const head = `${owner}:ci/failures-${fullSha}`;
              const prs = await github.rest.pulls.list({ owner, repo, head });
              if (prs.data && prs.data.length > 0) {
                const prNumber = prs.data[0].number;
                // Post a short comment linking to the gist
                const linkBody = `HTML final report attached as gist: ${gist.data.html_url}`;
                await github.rest.issues.createComment({ owner, repo, issue_number: prNumber, body: linkBody });
                console.log('Link comment posted on PR', prNumber);

                // Also post the HTML content as a comment (truncated if very large)
                const MAX_CHARS = parseInt(process.env.HTML_COMMENT_MAX_CHARS || '60000', 10) || 60000;
                let attachContent = content;
                let truncated = false;
                if (attachContent.length > MAX_CHARS) { attachContent = attachContent.slice(0, MAX_CHARS); truncated = true; }
                const htmlComment = `HTML final report (raw):\n\n---\n\n` + '```html\n' + attachContent + (truncated ? '\n\n... (truncated)' : '') + '\n```';
                await github.rest.issues.createComment({ owner, repo, issue_number: prNumber, body: htmlComment });
                console.log('HTML content posted as comment on PR', prNumber, truncated ? '(truncated)' : '');
              } else {
                console.log('PR not found for head', head);
              }
            } catch (e) {
              console.log('Error creating gist or commenting:', e.message || e);
            }
        env:
          GITHUB_SHA: ${{ github.sha }}

  analyze-unit:
    name: Analyze Unit Test Results
    runs-on: ubuntu-latest
    if: always()
    needs: unit-tests
    outputs:
      tests_ok: ${{ steps.summary.outputs.tests_ok }}
      failed_count: ${{ steps.summary.outputs.failed_count }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download unit test artifacts (Ubuntu)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-unit-tests-ubuntu-latest
          path: artifacts/unit/ubuntu
        continue-on-error: true

      - name: Download unit test artifacts (Windows)
        uses: actions/download-artifact@v4
        with:
          name: dotnet-unit-tests-windows-latest
          path: artifacts/unit/windows
        continue-on-error: true

      - name: Generate unit test summary (parse TRX)
        id: summary
        shell: bash
        run: |
          set -euo pipefail
          echo "Searching for TRX files under $(pwd)"
          mapfile -t files < <(find . -type f -name '*.trx' || true)
          if [ ${#files[@]} -eq 0 ]; then
            echo "No TRX files found; setting outputs and exiting"
            echo "tests_ok=false" >> $GITHUB_OUTPUT
            echo "failed_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          python3 scripts/ci-analyze.py
          failed=$(python3 -c "import json; print(json.load(open('tests-summary.json'))['failed'])")
          if [ "$failed" -eq 0 ]; then
            echo "tests_ok=true" >> $GITHUB_OUTPUT
          else
            echo "tests_ok=false" >> $GITHUB_OUTPUT
          fi
          echo "failed_count=$failed" >> $GITHUB_OUTPUT

      - name: Upload unit test summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-tests-summary
          path: |
            tests-summary.json
            tests-summary.txt

      - name: Run unit log analysis and enrich report (on failures)
        if: steps.summary.outputs.failed_count != '0'
        shell: bash
        run: |
          set -euo pipefail
          echo "Unit failures detected, gathering logs and generating enriched report"
          mkdir -p artifacts/enriched/unit
          echo "=== Unit Tests Enriched Report ===" > unit-tests-enriched-report.txt
          echo "See tests-summary.json for full summary." >> unit-tests-enriched-report.txt
          echo "\n--- Failures ---\n" >> unit-tests-enriched-report.txt
          cat tests-summary.txt >> unit-tests-enriched-report.txt

          # Create per-failure folders with details and log excerpts
          if command -v jq >/dev/null 2>&1; then
            jq -c '.failures[]' tests-summary.json | while read -r fail; do
              name=$(echo "$fail" | jq -r '.name' | sed 's/[^a-zA-Z0-9._-]/_/g' | cut -c1-200)
              msg=$(echo "$fail" | jq -r '.message')
              fileloc=$(echo "$fail" | jq -r '.file')
              outdir="artifacts/enriched/unit/$name"
              mkdir -p "$outdir"
              echo "Name: $name" > "$outdir/details.txt"
              echo "Message:" >> "$outdir/details.txt"
              echo "$msg" >> "$outdir/details.txt"
              echo "Source file: $fileloc" >> "$outdir/details.txt"
              # copy trx if present
              if [ -f "$fileloc" ]; then
                cp "$fileloc" "$outdir/" || true
              fi
              # collect log excerpts nearby
              grep -RIn --binary-files=text -E "Exception|ERROR|Timeout|Assertion|404|Not Found" . | sed -n '1,200p' > "$outdir/log-excerpts.txt" || true
            done
          else
            echo "jq not available; skipping per-failure folder creation" >> unit-tests-enriched-report.txt
          fi

          echo "Wrote unit-tests-enriched-report.txt and per-failure folders"

      - name: Upload unit enriched report (failures) (attempt 1)
        id: upload_unit_enriched_fail_1
        if: steps.summary.outputs.failed_count != '0'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt

      - name: Upload unit enriched report (failures) (attempt 2)
        id: upload_unit_enriched_fail_2
        if: steps.upload_unit_enriched_fail_1.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt

      - name: Upload unit enriched report (failures) (attempt 3)
        if: steps.upload_unit_enriched_fail_1.outcome != 'success' && steps.upload_unit_enriched_fail_2.outcome != 'success'
        uses: actions/upload-artifact@v4
        continue-on-error: true
        with:
          name: unit-tests-enriched-report-${{ github.job }}-${{ runner.os }}
          path: unit-tests-enriched-report.txt

  log-check:
    name: Conditional Log Inspection
    runs-on: ubuntu-latest
    needs: [analyze-results, analyze-unit, analyze-regression, analyze-integration, analyze-e2e]
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download server logs (Windows)
        uses: actions/download-artifact@v4
        with:
          name: server-log-Windows
          path: artifacts/logs/windows
        continue-on-error: true

      - name: Download server logs (Ubuntu)
        uses: actions/download-artifact@v4
        with:
          name: server-log-ubuntu-latest
          path: artifacts/logs/ubuntu
        continue-on-error: true

      - name: Quick log scan for exceptions and errors
        shell: bash
        run: |
          set -euo pipefail
          echo "Scanning downloaded logs for ERROR/Exception/Timeout entries"
          find artifacts -type f -name '*.log' -print -exec grep -nE "Exception|ERROR|Unhandled|Timeout|fail(ed)?" {} + || true
          echo "Downloading any per-analyzer enriched reports (if present)"
          find artifacts -type f -name '*-enriched-report*.txt' -print -exec sed -n '1,200p' {} + || true
          echo "Log inspection complete"
